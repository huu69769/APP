# PRD：打工日历 App（Android）

> 给 Claude Code 的开发需求文档。开发者（用户）是编程初学者：请按里程碑逐步完成，每完成一个里程碑就用简单的话说明做了什么、怎么在手机或网页上查看效果。

## 1. 产品概述

一个自用的安卓日历 app：在日历上记录日程、笔记和兼职班次，**打开首页就能看到本月的打工时长、工钱和空闲时间**。
交互参考「简单日历 - 每日时间表 app」：月历格子里直接显示每天的安排，点某一天进入详情，再添加内容。

- 平台：Android（APK 自行安装）；另外提供网页版，只用于预览界面
- 第一阶段不上架应用商店，但代码结构要为以后上架、云同步和多语言留好扩展空间

## 2. 技术方案

| 项目 | 选型 |
|---|---|
| 框架 | Expo（React Native）+ TypeScript，路由用 expo-router |
| 本地数据 | expo-sqlite。**必须做一层存储抽象（repository 层）**，网页版能换成浏览器存储，以后接云同步时也不用改界面代码 |
| 日期处理 | dayjs（含 timezone/utc 插件） |
| 农历 | lunar-javascript（或同类库）：农历日期、节气、传统节日 |
| 多语言 | i18next + react-i18next。**界面上不能出现写死的文字**，全部放进语言文件 |
| 通知 | expo-notifications（本地通知）；网页版不执行通知 |
| 测试 | Jest：所有计算函数（时长、工钱、空闲时间、周期）都要有单元测试 |
| 代码仓库 | GitHub 公开仓库 |

### 2.1 自动构建与发布（GitHub Actions）

push 到 main 分支后自动执行：

1. **网页版**：`npx expo export -p web`，部署到 GitHub Pages
   - 注意：Pages 的网址是 `/<仓库名>/` 子路径，要设置 `experiments.baseUrl`，否则打开会白屏
2. **APK**：在 Actions 里本地构建，不使用 EAS、不需要 Expo 账号。流程是 `npx expo prebuild -p android`，然后 `./gradlew assembleRelease`
   - 构建好的 APK 上传到 **GitHub Releases**，手机打开 Releases 页面就能下载
   - ⚠️ **签名密钥必须固定**：keystore 用 base64 存进 GitHub Secrets，每次构建都用同一个。密钥一变，新版就无法覆盖安装，只能卸载重装，**数据会全部丢失**
   - 生成 keystore 的步骤、需要用户在 GitHub 上手动设置 Secrets 的步骤，都要一步一步用简单的话说明
3. 每次构建自动增加版本号（versionCode）

## 3. 功能需求（第一版）

### 3.1 首页 = 统计栏 + 月历

**上方统计栏**（显示当前统计周期的数据）：
- 打工时长（小时）
- 工钱：可以在两种显示方式间切换
  - 模式 1：只显示总数
  - 模式 2：「已完成」+「预计」（班次结束时间早于现在的算已完成，其余算预计）
- 空闲天数（没有排班的天数）
- 空闲小时（周期内总小时数 − 打工占用的小时数，见 5.4）
- 点击统计栏进入统计详情页

**下方月历**：
- 左右滑动或按钮切换月份，有「回到今天」按钮
- 每个日期格子显示：日期数字、农历或节日小字（可在设置里关掉）、节假日标记、调休上班日标「班」
- 每个班次用兼职对应的颜色显示成一条色块，其他日程用小圆点表示
- 可以设置每周从周日还是周一开始
- 点某一天，进入当天详情页

### 3.2 兼职管理

- 可以添加多份兼职，每份的字段：名称、颜色、时薪、币种（CNY / JPY / USD，**默认 CNY**）、截止日（1–31，或「月末」）、默认休息分钟数
- 时薪变动后，**只影响之后新建的班次**，已有记录保持原来的时薪（见 5.3）

### 3.3 班次模板和打工录入

- **班次模板**：属于某一份兼职，比如「早班 9:00-14:00，休息 0 分钟」
- **录入方式**：
  1. 在当天详情页点模板，一键添加
  2. **批量排班**：在月历上选多个日期，套用同一个模板
  3. 手动填写（选兼职，填开始时间、结束时间、休息时间）
- 添加后可以单独修改时间、休息时间、备注，也可以删除
- 支持**跨夜班**（结束时间早于开始时间，就视为第二天结束，见 5.1）

### 3.4 日程和笔记

- 日程：标题、全天或指定时间段、备注、颜色、提醒（提前 N 分钟）
- 每天可以写一段笔记（纯文本）

### 3.5 提醒通知

- 班次和日程都可以设置提醒，比如提前 30 分钟、1 小时、前一天
- 第一次使用时申请通知权限（Android 13 及以上）
- 设置页里加一个说明：部分手机的省电模式会拦截通知，并告诉用户去哪里关闭这个限制
- 修改或删除日程、班次时，要同步更新或取消对应的通知

### 3.6 统计详情页

- **统计周期**可以切换：
  - 自然月：1 号到月底
  - 工资周期：按每份兼职自己的截止日计算，周期是「上个截止日的第二天」到「本期截止日」，以截止日所在的月份命名
- 按兼职分别显示：时长、工钱、出勤天数
- **年度累计收入**：1 月到 12 月，按币种分别合计，并显示每个月的明细
- 空闲时间：空闲天数、空闲小时

### 3.7 节假日和农历

- 节假日显示模式可以切换：中国 / 日本 / 中日合并 / 不显示
- 合并显示时，要能分清是哪个国家的节日（用颜色或小标签）
- **中国**：包括法定节假日和**调休上班日**（标「班」）。数据来源用 GitHub 上公开维护的 holiday-cn JSON
- **日本**：祝日和振替休日。数据来源用 holidays-jp 等公开的数据
- 数据要从网上获取并**保存在手机里**，没有网络时用已保存的数据；app 里也要预先打包当年的数据，作为第一次打开时的保底
- 每年年底中国公布新一年的安排后，app 能自动更新数据
- 农历显示可以开关（默认开启）：农历日期、节气、传统节日
- 结构要求：节假日按"国家"来接入，以后要加其他国家时，只需多接一个国家的数据

### 3.8 设置

- 界面语言：中文（默认）/ 日本語。以后会加 English，只需要多一个语言文件
- **界面语言和节假日的国家是两个独立的设置**
- 默认币种：CNY（默认）/ JPY / USD
- 统计周期、工钱显示方式、节假日模式、农历开关、每周起始日
- 备份：导出、导入

### 3.9 备份

- 可以把全部数据导出成一个 JSON 文件，通过安卓的分享功能保存到文件夹或云盘
- 导入 JSON 文件后恢复数据，导入前要让用户确认
- 如果超过 30 天没有备份，在首页提示一次

## 4. 数据结构

所有数据表都要有这些共用字段，为以后的云同步做准备：
- `id`：UUID（**不要用自增编号**）
- `createdAt`、`updatedAt`：ISO 格式时间
- `deletedAt`：删除时只做标记（软删除），列表里不显示已删除的记录

| 表 | 字段 |
|---|---|
| jobs | name, color, hourlyWage, currency, cutoffDay (1–31 或 null=月末), defaultBreakMinutes |
| shift_templates | jobId, name, startTime (HH:mm), endTime (HH:mm), breakMinutes |
| shifts | jobId, date (YYYY-MM-DD 本地日期), startTime, endTime, breakMinutes, **wageSnapshot**, **currencySnapshot**, note, reminderMinutesBefore |
| events | date, title, allDay, startTime, endTime, color, note, reminderMinutesBefore |
| day_notes | date, content |
| settings | 键值对 |
| holidays_cache | country, year, data (JSON), fetchedAt |

- 金额以「最小单位的整数」保存：JPY 存 1 = 1 日元；CNY/USD 存 1 = 0.01（分 / 美分）。**不用浮点数算钱**
- 预留字段，第一版不用：shifts 的 `premiumRules`（以后做夜班、周末加价）

## 5. 计算规则和已知的坑（必须处理，并写测试）

1. **跨夜班**：结束时间 ≤ 开始时间，就视为第二天结束。时长正确跨过 0 点；这个班算在开始那天
2. **休息扣除**：实际时长 = 结束 − 开始 − 休息分钟数；结果小于 0 时报错，不能保存
3. **时薪快照**：工钱 = 实际时长 × wageSnapshot。改兼职的时薪，不影响已有的班次
4. **空闲小时**：不设"可用时间"，**不在打工的时间都算空闲**。每天的空闲 = 24 小时 − 当天打工占用的时间
   - 打工占用的时间按班次的开始到结束计算，**包括休息时间**（休息时人还在工作的地方，不算空闲；工钱仍然按扣除休息后的时长计算）
   - 同一天的班次如果重叠，先合并再扣除，不重复扣
   - 跨夜班超过当天 24 点的部分，算进第二天
   - 日程不扣空闲时间（日程是用空闲时间安排的事）
5. **空闲天数**：统计周期内没有任何班次的天数
6. **日期错一天**：日期一律用本地的 `YYYY-MM-DD` 字符串保存和比较，不要用 UTC 时间戳换算日期
7. **多币种合计**：不同币种**不做换算、不相加**，按币种分别显示（比如「¥3,200 + $50」）。JPY 显示整数，CNY/USD 显示两位小数
8. **截止日超过当月天数**：比如设了 31 日截止，遇到 2 月就按月末处理
9. **网页版限制**：网页版的数据和手机上的数据是分开的；网页上不做通知，相关按钮给出提示
10. **签名密钥**：见 2.1，绝对不能变
11. **敏感信息**：公开仓库里不能出现密钥或密码，一律放在 GitHub Secrets

## 6. 以后再做（第一版不做，但结构上要预留）

- 云同步和登录（E2，比如 Supabase）
- 待办清单
- 夜班、周末加价规则
- 工时取整（比如 15 分钟为单位）
- 英文界面，更多国家的节假日
- 桌面小组件
- 上架 Google Play

## 7. 开发里程碑

每完成一个，都要告诉用户怎么查看效果。

- **M1 框架**：Expo 项目、多语言框架、存储抽象层、GitHub Actions（网页版 + APK + Releases），首页显示空白月历
- **M2 打工**：兼职管理、班次模板、录入（一键、批量、手动），月历上显示班次色块
- **M3 统计**：统计栏、统计详情、年度累计、两种周期、两种工钱显示方式，并写好计算函数的测试
- **M4 日程**：日程、笔记、提醒通知
- **M5 节假日**：中国、日本节假日和调休、农历
- **M6 收尾**：备份导出和导入、设置页、日文翻译检查
